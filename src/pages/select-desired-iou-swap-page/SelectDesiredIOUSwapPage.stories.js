import React from 'react';
import SelectDesiredIOUSwapPage from './SelectDesiredIOUSwapPage';

export default {
  title: 'Pages/SelectDesiredIOUSwapPage',
  component: SelectDesiredIOUSwapPage,
};

const Template = (args) => <SelectDesiredIOUSwapPage {...args} />;

export const Base = Template.bind({});
Base.args = {};
